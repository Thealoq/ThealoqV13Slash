const config = global.config
class Commands {
    constructor(client) {
        this.name = "register"
        this.description = "register for user"
    }
    execute(ctx, client) {
      
    }
}

module.exports = Commands